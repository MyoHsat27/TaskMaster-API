import Task from "../../models/task.js";
import { TaskCreateObject } from "../../types/task.js";
import { transformToObjectId } from "../../helpers/mongoHelper.js";

export const getTaskById = async (id: string) => {
    try {
    } catch (error: any) {
        throw new Error(error);
    }
};

export const getAllTasks = async (task: TaskCreateObject) => {
    try {
    } catch (error: any) {
        throw new Error(error);
    }
};

export const createTask = async (task: TaskCreateObject) => {
    try {
    } catch (error: any) {
        throw new Error(error);
    }
};

export const updateTask = async (task: TaskCreateObject) => {
    try {
    } catch (error: any) {
        throw new Error(error);
    }
};

export const deleteTask = async (task: TaskCreateObject) => {
    try {
    } catch (error: any) {
        throw new Error(error);
    }
};
