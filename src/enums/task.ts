export const enum TaskStatusType {
    PENDING = "income",
    INPROGRESS = "in-progress",
    COMPLETE = "completed"
}

export const enum TaskPriorityType {
    LOW = 1,
    MEDIUM = 2,
    HIGH = 3
}
